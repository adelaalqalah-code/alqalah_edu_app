# 📊 شاشة استيراد وتصدير Excel - دليل التنفيذ الكامل
# Alqalah Edu App - Excel Import/Export Module

## 📁 الملفات المنشأة

### 1. lib/core/services/excel_service.dart
الخدمة الأساسية لقراءة وكتابة ملفات Excel:
- `pickAndParseGradesExcel()` - اختيار وتحليل ملف Excel
- `saveImportedGrades()` - حفظ الدرجات في Supabase
- `exportGradesToExcel()` - تصدير الدرجات إلى Excel
- `downloadTemplate()` - تحميل قالب فارغ

### 2. lib/screens/professor/import_export_screen.dart
الشاشة الرئيسية:
- اختيار نوع الدرجة (تكليف/مشاركة/واجب)
- اختيار المساق/المادة
- أزرار: استيراد / تصدير / قالب
- شريط تقدم أثناء المعالجة
- بطاقة النتيجة (نجاح/فشل)

### 3. lib/widgets/grade_import_preview.dart
ويدجت المعاينة:
- جدول بيانات مباشر
- تمييز الصفوف الصالحة/غير الصالحة
- أزرار: حفظ / إلغاء

## 🔄 سير العمل (Workflow)

1. اختيار نوع الدرجة + المساق
2. الضغط على "استيراد من Excel"
3. اختيار الملف (.xlsx)
4. قراءة وتحليل الأعمدة تلقائياً
5. معاينة البيانات (مع تمييز الأخطاء)
6. الضغط على "حفظ"
7. التحقق من وجود الطالب + عدم التثبيت
8. الحفظ في Supabase + تسجيل في Audit Log

## 📋 هيكل ملف Excel المدعوم

| رقم التأكيد | اسم الطالب | الدرجة | النوع | ملاحظات |
|------------|-----------|--------|-------|---------|
| CS-2026-0001 | أحمد الصبري | 85 | تكليف | - |
| CS-2026-0002 | سارة عبدالله | 92 | مشاركة | ممتاز |

## ⚡ مميزات التحقق

- ✅ البحث التلقائي عن الأعمدة (يدعم عدة تسميات)
- ✅ قراءة "85/100" واستخراج الدرجة
- ✅ التحقق من وجود الطالب في قاعدة البيانات
- ✅ منع التعديل على الدرجات المثبتة
- ✅ سجل أخطاء مفصل
- ✅ دعم حتى 1000+ صف في ملف واحد

## 🔧 التبعيات المطلوبة (pubspec.yaml)

```yaml
dependencies:
  excel: ^4.0.2
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  share_plus: ^7.2.2
  supabase_flutter: ^2.5.0
  flutter_riverpod: ^2.5.1
```

## 🗄️ SQL إضافي مطلوب

```sql
-- أنواع الدرجات الافتراضية
INSERT INTO grade_types (name, color, sort_order) VALUES
('تكليف', '#3B82F6', 1),
('مشاركة', '#8B5CF6', 2),
('واجب', '#F59E0B', 3),
('نصفي', '#EF4444', 4),
('نهائي', '#10B981', 5);
```

## 📱 استخدام الشاشة

```dart
// في BottomNavigationBar أو القائمة
Navigator.push(
  context,
  MaterialPageRoute(builder: (_) => const ImportExportScreen()),
);
```
